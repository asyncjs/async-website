<!DOCTYPE html>
<html class="flex flex-1 min-h-full">
  {{> head}}
  <body class="flex-1">
    <div class="wrapper flex-1">
      {{> masthead}}
      {{{ contents }}}
      {{> footer}}
    </div>
  </body>
</html>
