<!DOCTYPE html>
<html class="flex min-h-full">
  {{> head}}
  <body class="flex-1">
    <div class="wrapper flex-1">
      {{> masthead}}
      {{{ contents }}}
      {{> footer}}
    </div>
  </body>
</html>
