{{#if link}}
  <a class="meetup-link flex items-center gap-x-1" target="_blank" href="{{ link }}" title="View {{#if title}}{{ title }} {{/if}}on Meetup">
    {{ text }}{{{ iconmonstr 'meetup' }}}
  </a>
{{/if}}
